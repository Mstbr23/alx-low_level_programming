#include "main.h"
/**
 * main - Entry point
 * @str: char to show.
 * Return: Always 0 (Success)
 */
int _puts(char *str)
{
	str = "\"Programming is like building a multilingual puzzle";
	_puts(str);
	return (0);
}
