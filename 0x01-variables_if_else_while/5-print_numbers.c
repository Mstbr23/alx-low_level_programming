#include <stdio.h>
/**
  * main - Entry point
  * Description: 'a C program that print a specific text'
  * Return: Always  0 (Success)
  */
int main(void)
{
	int a;

	for (a = 0; a <= 100; a++)
		putchar(a);
	putchar('\n');
	return (0);
}
